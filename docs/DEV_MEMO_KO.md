# 개발 메모 (한글)

## 2026-03-02 중요 반영
- 기록 메인화면 AI 리포트 순서/상태 정리
- 데이터 부족 알림(`leck_data_alert`) 여백을 피그마 기준(350x200, 중앙정렬)으로 반영
- AI 리포트 상세 카드에 `AI` 뱃지 및 소스 뱃지(실제 AI/서버 Fallback/로컬 Fallback) 반영
- `N월 키워드` 추출을 명사 중심으로 강화
  - 조사 제거
  - 비명사 어미/잡단어 필터 확대 (`계속`, `많이`, `말고`, `돼요`, `~고/~어/~나` 등)
  - 키워드가 없으면 기본 1개(`기록`)만 노출
- 문장다듬기 기능은 MVP에서 UI 비노출 처리
  - 다듬기 결과는 분리 저장소(`PolishDraftStore`)에 별도 저장
  - 향후 기능 재오픈 시 재사용 가능하도록 유지
